
function addMessage(messageHTML) {
  const messages = document.querySelector('.main-container');
  messages.insertAdjacentHTML('afterbegin', messageHTML);
}


addMessage('<%= j render "message", message: @message %>');
